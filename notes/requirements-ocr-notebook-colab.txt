# Зависимости для notes/statisctics.ipynb (GOT-OCR2.0 + метрики).
# verovio — подтягивается remote code модели (иначе ImportError при from_pretrained).

jiwer>=3.0.0
pandas>=2.0.0
tqdm>=4.66.0
Pillow>=10.0.0
safetensors>=0.4.0
accelerate>=0.26.0
sentencepiece>=0.1.99
protobuf>=4.25.0
verovio>=3.0.0
transformers>=4.40.0,<6

# torch в Colab обычно уже установлен (CUDA).
