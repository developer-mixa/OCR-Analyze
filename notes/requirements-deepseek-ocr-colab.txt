# Зависимости для notes/statisctics.ipynb (DeepSeek-OCR + метрики).
# В Colab обычно достаточно первой строки + jiwer; torch часто уже установлен.

jiwer>=3.0.0
pandas>=2.0.0
tqdm>=4.66.0
Pillow>=10.0.0
safetensors>=0.4.0
accelerate>=0.26.0
sentencepiece>=0.1.99
protobuf>=4.25.0
einops>=0.7.0
addict>=2.4.0
easydict>=1.10

# Модель и трансформеры (версии подберите под CUDA в Colab; при конфликте снимите закрепление).
transformers>=4.45.0

# torch: в Colab ставьте через встроенную среду или:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124

# Опционально (ускорение на подходящих GPU; без пакета ноутбук переключится на sdpa):
# flash-attn --no-build-isolation
