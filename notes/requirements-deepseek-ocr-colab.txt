# DeepSeek-OCR: версии как в официальном README модели (HF / deepseek-ai/DeepSeek-OCR).
# Иначе remote code может падать на импортах вроде LlamaFlashAttention2 при других версиях transformers.

jiwer>=3.0.0
pandas>=2.0.0
tqdm>=4.66.0
Pillow>=10.0.0
safetensors>=0.4.0
accelerate>=0.26.0
sentencepiece>=0.1.99
protobuf>=4.25.0
einops>=0.7.0
addict>=2.4.0
easydict>=1.10

transformers==4.46.3
tokenizers==0.20.3

# Официально: torch==2.6.0 — в Colab обычно уже есть CUDA-сборка; при конфликтах см. README PyTorch.
# flash-attn==2.7.3 — опционально, pip install --no-build-isolation (сборка может занять много времени).
